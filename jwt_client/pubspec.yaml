name: jwt_client
description: A client for the JWT-authenticated "Hello World" server.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.8

dependencies:
  http: ^1.2.0
  dart_jsonwebtoken: ^2.8.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
